---
hide_body: false
is_partial: false
fields:
- type: text
  label: Teacher's name
  name: name
  description: Enter the first and last name of the teacher
  config:
    required: true
- type: file
  name: image
  label: Teacher's photo
  description: Upload or set an image of the teacher
  config:
    maxSize: '2'
